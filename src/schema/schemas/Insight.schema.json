{
    "$id": "https://microsoft.com/is/Insight",
    "$schema": "http://json-schema.org/draft-07/schema",
    "description": "insight node",
    "allOf": [
        {"$ref": "core#/definitions/core"},
        {"$ref": "#/$defs/body"},
        {"$ref": "core#/definitions/requirement"}
    ],

    "$defs": {
        "body": {
            "type": "object",
            "required": [ "kind" ],
            "properties": {
                "kind": {
                    "enum": [ "Add", "Add/Fix", "Fix", "Insight", "Pattern" ]
                }
            }
        }
    }
}
